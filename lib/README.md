# `lib/`

Shared utilities used across the app (client and server).

## Examples

- Environment variable parsing/validation (non-secret values can be shared with client)
- Auth helpers (server-side wrappers around Clerk)
- MongoDB connection helpers
- Error normalization utilities
