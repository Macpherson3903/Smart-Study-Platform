# `components/`

Reusable React components.

## Conventions

- Put **generic UI primitives** in `components/ui/` (buttons, inputs, cards).
- Put **feature-specific components** in `components/features/<feature>/`.
- Keep components **presentational** when possible; business logic belongs in `server/` and `lib/`.
