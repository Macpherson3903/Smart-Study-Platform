# `hooks/`

Client-side React hooks.

## Conventions

- Hooks in this folder must be **safe for the browser** (no secrets, no direct DB access).
- Server-only logic belongs in `server/` (services) and `lib/` (shared utilities).
