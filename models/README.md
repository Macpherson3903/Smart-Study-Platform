# `models/`

Data contracts / persistence models.

## Conventions

- Define TypeScript types for documents stored in MongoDB.
- Keep models independent from business logic; business rules live in `server/services`.
